# Points to add:

1. Data Collection and Cleaning and Chunking Process
2. Data example
3. Experimental setup
4. Saving logic used (Chroma DB)
5. Evaluation and Results (Chroma DB vs FAISS and LSH) and (Different chunking sizes) and (Given a UPG and background text, retrieve top 3 chunks and check if the UPG is present in the retrieved chunks). Also, compare results of without RAG and just the base model, with rag + sft, with just sft.
6. Which type of RAG (self-rag, graph-rag, etc) was implemented and how.
7. All the hyperparameters of the RAG and the model.